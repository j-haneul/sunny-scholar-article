{
  "name": "sunny-scholar-archive",
  "version": "1.0.0",
  "scripts": {
    "start": "serve public",
    "build": "mkdir -p out && cp public/index.html out/"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
